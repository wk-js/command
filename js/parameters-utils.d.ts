export declare class ParametersUtils {
    static indexOf(parameters: string[], parameter: string): number;
    static get(parameters: string[], parameter: string): string | boolean;
    static has(parameters: string[], parameter: string): number;
}
